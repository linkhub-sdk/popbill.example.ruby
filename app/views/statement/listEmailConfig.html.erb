<div id="content">
  <p class="heading1">Response</p>
  <br/>
  <fieldset class="fieldset1">
    <legend><%= request.env['PATH_INFO'] %></legend>
    <fieldset class="fieldset2">
      <legend>알림메일 전송목록 조회</legend>
      <ul>
        <% @Response.each_with_index do |emailConfig| %>
          <li>
            <% if emailConfig["emailType"] == "SMT_ISSUE" %>
              SMT_ISSUE (수신자에게 전자명세서가 발행 되었음을 알려주는 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "SMT_ACCEPT" %>
              SMT_ACCEPT (발신자에게 전자명세서가 승인 되었음을 알려주는 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "SMT_DENY" %>
              SMT_DENY (발신자에게 전자명세서가 거부 되었음을 알려주는 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "SMT_CANCEL" %>
              SMT_CANCEL (수신자에게 전자명세서가 취소 되었음을 알려주는 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "SMT_CANCEL_ISSUE" %>
              SMT_CANCEL_ISSUE (수신자에게 전자명세서가 발행취소 되었음을 알려주는 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
          </li>
        <% end %>
      </ul>
    </fieldset>
  </fieldset>
</div>