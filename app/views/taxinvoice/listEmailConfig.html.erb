<div id="content">
  <p class="heading1">Response</p>
  <br/>
  <fieldset class="fieldset1">
    <legend><%= request.env['PATH_INFO'] %></legend>
    <fieldset class="fieldset2">
      <legend>알림메일 전송목록 조회</legend>
      <ul>
        <% @Response.each_with_index do |emailConfig| %>
          <li>
            <% if emailConfig["emailType"] == "TAX_ISSUE" %>
              TAX_ISSUE (공급받는자에게 전자세금계산서 발행 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_ISSUE_INVOICER" %>
              TAX_ISSUE_INVOICER (공급자에게 전자세금계산서 발행 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_CHECK" %>
              TAX_CHECK (공급자에게 전자세금계산서 수신확인 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_CANCEL_ISSUE" %>
              TAX_CANCEL_ISSUE (공급받는자에게 전자세금계산서 발행취소 메일) : <%= emailConfig["sendYN"] %>
            <% end %>

            <% if emailConfig["emailType"] == "TAX_SEND" %>
              TAX_SEND (공급받는자에게 [발행예정] 세금계산서 발송 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_ACCEPT" %>
              TAX_ACCEPT (공급자에게 [발행예정] 세금계산서 승인 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_ACCEPT_ISSUE" %>
              TAX_ACCEPT_ISSUE (공급자에게 [발행예정] 세금계산서 자동발행 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_DENY" %>
              TAX_DENY (공급자에게 [발행예정] 세금계산서 거부 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_CANCEL_SEND" %>
              TAX_CANCEL_SEND (공급받는자에게 [발행예정] 세금계산서 취소 메일) : <%= emailConfig["sendYN"] %>
            <% end %>

            <% if emailConfig["emailType"] == "TAX_REQUEST" %>
              TAX_REQUEST (공급자에게 세금계산서를 발행요청 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_CANCEL_REQUEST" %>
              TAX_CANCEL_REQUEST (공급받는자에게 세금계산서 취소 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_REFUSE" %>
              TAX_REFUSE (공급받는자에게 세금계산서 거부 메일) : <%= emailConfig["sendYN"] %>
            <% end %>

            <% if emailConfig["emailType"] == "TAX_TRUST_ISSUE" %>
              TAX_TRUST_ISSUE (공급받는자에게 전자세금계산서 발행 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_TRUST_ISSUE_TRUSTEE" %>
              TAX_TRUST_ISSUE_TRUSTEE (수탁자에게 전자세금계산서 발행 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_TRUST_ISSUE_INVOICER" %>
              TAX_TRUST_ISSUE_INVOICER (공급자에게 전자세금계산서 발행 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_TRUST_CANCEL_ISSUE" %>
              TAX_TRUST_CANCEL_ISSUE (공급받는자에게 전자세금계산서 발행취소 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_TRUST_CANCEL_ISSUE_INVOICER" %>
              TAX_TRUST_CANCEL_ISSUE_INVOICER (공급자에게 전자세금계산서 발행취소 메일) : <%= emailConfig["sendYN"] %>
            <% end %>

            <% if emailConfig["emailType"] == "TAX_TRUST_SEND" %>
              TAX_TRUST_SEND (공급받는자에게 [발행예정] 세금계산서 발송 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_TRUST_ACCEPT" %>
              TAX_TRUST_ACCEPT (수탁자에게 [발행예정] 세금계산서 승인 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_TRUST_ACCEPT_ISSUE" %>
              TAX_TRUST_ACCEPT_ISSUE (수탁자에게 [발행예정] 세금계산서 자동발행 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_TRUST_DENY" %>
              TAX_TRUST_DENY (수탁자에게 [발행예정] 세금계산서 거부 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_TRUST_CANCEL_SEND" %>
              TAX_TRUST_CANCEL_SEND (공급받는자에게 [발행예정] 세금계산서 취소 메일) : <%= emailConfig["sendYN"] %>
            <% end %>

            <% if emailConfig["emailType"] == "TAX_CLOSEDOWN" %>
              TAX_CLOSEDOWN (거래처의 휴폐업 여부 확인 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "TAX_NTSFAIL_INVOICER" %>
              TAX_NTSFAIL_INVOICER (전자세금계산서 국세청 전송실패 안내) : <%= emailConfig["sendYN"] %>
            <% end %>

            <% if emailConfig["emailType"] == "TAX_SEND_INFO" %>
              TAX_SEND_INFO (전월 귀속분 [매출 발행 대기] 세금계산서 발행 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
            <% if emailConfig["emailType"] == "ETC_CERT_EXPIRATION" %>
              ETC_CERT_EXPIRATION (팝빌에서 이용중인 공인인증서의 갱신 메일) : <%= emailConfig["sendYN"] %>
            <% end %>
          </li>
        <% end %>
      </ul>
    </fieldset>
  </fieldset>
</div>